import { PlayerLeaderBoard } from "./leaderboard";

/* initialize board */
const board = new PlayerLeaderBoard();


/* unit tests here */
describe("leaderboard", () => {
    it("can add node", () => {
        //
    });
});
